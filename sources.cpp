#include <iostream>
#include <string>
using namespace std;

int main() {
	int var;
	cout << "Please choose mode:";
	cin >> var;
	if (var = 1); {
		int x, y;
		int sum;
		cout << "Type your number:";
		cin >> x;
		cout << "Type another:";
		cin >> y;
		sum = x + y;
		cout << "The result is:" << sum;
		return 0;
	}
	if (var = 2); {
		int x, y;
		int sum;
		cout << "Type your number:";
		cin >> x;
		cout << "Type another:";
		cin >> y;
		sum = x - y;
		cout << "The result is:" << sum;
		return 0;
	}
	
}